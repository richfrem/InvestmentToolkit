# Agent Skills & Capabilities

## Skill: Repo Sanitation
The agent is trained to manage the lifecycle of Git Worktrees. It follows a "Merge and Destroy" policy to ensure the local environment stays synchronized with the main branch and contains zero zombie directories.
