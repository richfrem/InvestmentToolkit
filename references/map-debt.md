# Map Debt Ledger

Persistent tracking of architectural friction, structural anomalies, and unclosed loops across sessions.

| ID | Title | Status | Severity | Repeat | First Seen | Description | Resolution Commit |
|---|---|---|---|---|---|---|---|
| DEBT-20260831-01 | Inline Python/SQL fallback due to missing `portfolio_io.py` CLI query primitives | RESOLVED | Tier 0 | 1 | 2026-08-31 | Valuation triage on GOOG/ZS led to ad-hoc inline Python/SQL execution because `portfolio_io.py` lacked standalone CLI query flags for ticker holding status and strategy pillar resolution. | Added `--ticker`, `--pillars`, and `--json` CLI flags to `portfolio_io.py`; updated `update-stock-analysis/SKILL.md` Step 0.1 to strictly invoke canonical CLI and explicitly forbid inline Python/SQL. |
| DEBT-20260901-01 | Foreign key constraint failure during stock intake/refresh on `pillar_id`/`sub_strategy_id` | RESOLVED | Tier 0 | 1 | 2026-09-01 | `stock_intake_persist.py` failed with `sqlite3.IntegrityError: FOREIGN KEY constraint failed` when updating existing tickers (CORZ/BE) due to missing schema validation on incoming pillar IDs. | Hardened `stock_intake_persist.py` with schema foreign key validation, auto-inheriting existing valid DB values or defaulting gracefully; updated `wiki/playbook-stock-valuation-lifecycle.md` (Invariant D). |
| DEBT-20260902-01 | `portfolio_io.py:load_thesis_holdings` omitted `standing_decision_*` columns causing prompt generator fallback | RESOLVED | Tier 0 | 1 | 2026-09-02 | `generate_grok_prompt.py` attempted to read `standingDecisionReason` from `load_thesis_holdings()`, which omitted the column from the returned dict, silently falling back to scenario risks. | Updated `portfolio_io.py:load_thesis_holdings()` to populate `standingDecisionReason` & `standingDecisionType`; sanitized Markdown table cells and replaced pipe join delimiter with semicolon. |


| DEBT-20260902-03 | verify_thesis_sync.py flagged 0% target weight watchlist tickers as missing thesis docs | RESOLVED | Tier 2 | 1 | 2026-09-02 | In verify_thesis_sync.py line 98, the filter checked `(target_weight > 0 or lifecycle_status)` which swept 19 pure watchlist/exit tickers with target_weight=0 (AAPL, AMZN, etc.) into the mandatory thesis documentation check, failing verification erroneously. | Fixed filter to exempt `lifecycle_status in ('watchlist', 'avoid', 'exit')` when target_weight is 0, restoring clean verification pass. |
| DEBT-20260902-04 | Missing raw financials file in update-stock-analysis pipeline | RESOLVED | Tier 1 | 1 | 2026-09-02 | In update-stock-analysis execution, dcf_scenarios.py failed with FileNotFoundError: temp/evaluations/APH_raw.json because step 1 output was not redirected to file before invoking step 3. | Pre-create temp/evaluations directory and save fetch_financials output to temp/evaluations/{TICKER}_raw.json systematically before running dcf_scenarios.py. |
| DEBT-20260902-05 | Target allocation drift exceeding 100% and ScreenerTable lack of urgent action triage | RESOLVED | Tier 1 | 1 | 2026-09-02 | In domain_model.sqlite, target weights accumulated to 109.54% after adding CRDO and VST without pro-rata normalization. Meanwhile, the Screener UI displayed 0 actionable items, monotonous MAINTAIN tags, and upside-only sorting, making urgent buy/trim actions invisible. | Added check_target_weight_invariant() enforcing 100.0% +/- 0.05% in verify_portfolio_invariants.py with pytest tests, re-calibrated SQLite target weights to exactly 100.0%, and added Executive Action Triage Bar with priority-based sorting in ScreenerTable.tsx and actionColors.ts. |
| DEBT-20260902-06 | Screener showed $0.00 price and — upside for all watchlist/holding tickers with no TA sweep data | RESOLVED | Tier 1 | 1 | 2026-09-02 | ScreenerTable.tsx used p.snapshot?.price as primary price source — 0 for any ticker never run through a TA sweep. Additionally fairValue was null for tickers lacking a DCF projection, making upside/gainLoss always — for the majority of watchlist entries. | Unified price sourcing through fetch_portfolio_heatmap.py (TV CDP primary, yfinance fallback): added analyst_target_mean field to heatmap output; ScreenerTable now reads heatmapMap[ticker].price as primary currentPrice and uses analyst_target_mean as fair value fallback when no DCF projection exists. |
