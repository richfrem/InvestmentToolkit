# Task 0003: Refresh stale DCF projections and standardize temp output paths

## Objective

Two cleanup items surfaced during Phase 1 test suite work:

**1. Stale DCF projections (23 files)**
`validate_all_projections.py` found 23 projection files where stored `aiThesis.action` or `aiThesis.fairValue` no longer matches the DCF math — prices have drifted since valuations were run. These need a re-sweep so the screener action labels and fair value displays are trustworthy.

**2. Temp output path discipline**
Scripts (`fetch_financials.py`, `dcf_scenarios.py`, etc.) write intermediate files directly to `/tmp/` root:
- `/tmp/ALAB_raw.json`
- `/tmp/ALAB_dcf_result.json`
- `/tmp/ALAB_projection.json`

These should go into a dedicated subfolder `InvestmentToolkit/temp/` (or `/tmp/investmenttoolkit/`) so temp artifacts are namespaced, easy to find, and easy to gitignore/clean.

## Acceptance Criteria

### Part 1 — Stale projections
- [ ] Run `/evaluate-stock` re-sweep on all 23 flagged tickers (AMD, APLD, ASML, BITF, BTDR, BW, CAKE, CIFR, CLSK, HUMN, INTC ×2, IONQ, NKE, NVDA, OKLO, PANW, PLTR, RIOT, SEI, SNDK, TSEM, TSLA)
- [ ] `python3 tests/validate_all_projections.py` exits 0 (or with only acceptable ACCUMULATE/TRIM/REVIEW actions that are intentional portfolio overrides, not DCF signal mismatches)

### Part 2 — Temp path standardization
- [ ] Audit all scripts that write to `/tmp/` and identify the write pattern
- [ ] Decide on canonical temp dir: repo-local `temp/` (gitignored) vs `/tmp/investmenttoolkit/`
- [ ] Update all affected scripts to write to the chosen subfolder
- [ ] Add `temp/` to `.gitignore` if using repo-local approach
- [ ] Confirm `dcf_scenarios.py --pretty` and `fetch_financials.py` output to new path in test run

## Notes

- Stale ticker list generated by `python3 tests/validate_all_projections.py` on 2026-05-17
- INTC has two entries — one has `fairValue: 75.00` with `scenarioPrice` sum of 0 (likely a legacy entry with missing scenario data), the other has -48% upside marked HOLD
- The 23 count may shrink as prices move — always re-run the validator to get the current list before starting the sweep
- NVDA is also in the list (recomputed FV = 0.00) — likely another legacy entry with missing scenarioPrice fields; check before re-running full valuation
