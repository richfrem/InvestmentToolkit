---
name: x-news-sweep
description: Generates a live Grok/X.com prompt from target-portfolio.json, receives Grok's structured response, gates each recommendation against DCF and the 8 hard gates, then applies approved target changes.
type: command
---

# /x-news-sweep

Invoke the `portfolio-advisor:x-news-sweep` skill.
