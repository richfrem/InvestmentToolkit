{
  "name": "tv-cdp-client",
  "version": "1.0.0",
  "type": "module",
  "description": "TradingView CDP client — owned copy for InvestmentToolkit. No external MCP server needed.",
  "scripts": {
    "test": "node --experimental-vm-modules node_modules/.bin/jest"
  },
  "dependencies": {
    "chrome-remote-interface": "^0.33.0"
  },
  "devDependencies": {
    "jest": "^29.7.0"
  }
}
