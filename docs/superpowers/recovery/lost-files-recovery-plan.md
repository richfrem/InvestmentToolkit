# Lost Files Recovery Plan

Generated: 2026-07-18. A destructive `git reset --hard 1c3d882` was run during branch cleanup
without first checking `git status` or stashing uncommitted changes. This discarded
pre-existing uncommitted working-directory changes the user had explicitly asked (twice) to be
committed and pushed instead.

**Do not regenerate anything listed below without explicit user approval.** This document is a
plan for the user to review and approve, not a record of actions already taken.

**Do not overwrite any newly created file without checking `git status` and file existence
first**, per corrective instructions §8/§11.

---

## Affected Files

| # | File | Tracked? | Nature (generated / source-of-truth) | Likely regeneration command | Regenerable by ordinary daily-loop scripts? | Likely permanently lost? |
|---|---|---|---|---|---|---|
| 1 | `context/events.jsonl` | Tracked | Generated — Agentic-OS session/lock telemetry append log (`.agents/skills/os-init/templates/kernel.py`'s `emit_event()`); unrelated to the SQLite migration domain | None specific — new entries append automatically as future agent sessions run | Yes, implicitly (any future session appends fresh entries) | The specific ~3 lost lines: yes, permanently. The file's ongoing utility: no impact |
| 2 | `investment_screener/backend/data/predictions.jsonl` | Tracked | Source-of-truth-adjacent — harvested prediction-ledger entries written by `harvest_predictions.py`/`earnings_expectations.py` (append-only ledger per `prediction_ledger.py`'s own docstring) | `python3 investment_screener/backend/py_services/harvest_predictions.py` (re-harvests from current live data — **will not reproduce the exact historical entries that existed at harvest time**, only current-state equivalents) | Partially — re-running harvest produces new entries, not the identical lost ~85 lines | Likely yes for the exact lost content; a current re-harvest is not identical data |
| 3 | `investment_screener/backend/data/ta-sweep-results.json` | Tracked | Generated — latest TA scan cache, explicitly documented as "overwritten on every write, no history retained" (design spec's own audit finding) | `python3 plugins/tradingview/scripts/ta_sweep_batch.py` (requires TradingView Desktop CDP running) | Yes — this is exactly what the script is for | No — pure cache, safe to regenerate |
| 4 | `investment_screener/backend/data/theses/target-portfolio.json` | Tracked | Source-of-truth — target weights, standing decisions, user-confirmed portfolio strategy | None generic — this file is user-edited/agent-edited directly, not mechanically regenerated | No | Unclear — depends on what the specific lost edit was; not identified in this pass |
| 5 | `investment_screener/backend/data/thesis_breaker_state.json` | Tracked | Generated — breaker trigger/streak tracking state | `python3 plugins/portfolio-advisor/scripts/update_thesis_breakers.py` or equivalent breaker-check step in the daily loop | Yes, but historical trigger timestamps in the lost delta may not reproduce identically | Partially — current state regenerates, historical timing detail may not |
| 6 | `investment_screener/backend/data/theses/investment_thesis.md` | Tracked | Hybrid — generated `AUTO_UPDATE` block over hand-authored prose | `python3 plugins/portfolio-advisor/scripts/generate_portfolio_blueprint.py --write` | Yes, for the `AUTO_UPDATE` block specifically | No — regenerable block; hand-authored prose sections were untouched by the reset (only uncommitted diffs were lost, and this file's prose sections don't appear to have been mid-edit) |
| 7 | `investment_screener/backend/data/theses/sub_strategies/*.md` (11 files: `applied_ai`, `asi_race`, `cybersecurity`, `metabolic_rewriting`, `photonics_optical`, `power_infrastructure`, `quality_saas`, `quantum_computing`, `robotics_automation`, `sovereign_finance`, `space_defense`, `strategic_reserve`) | Tracked | Hybrid — generated `AUTO_UPDATE` block over hand-authored prose | `python3 plugins/portfolio-advisor/scripts/generate_sub_strategy_blocks.py` | Yes, for the `AUTO_UPDATE` block specifically | No — same reasoning as #6 |
| 8 | `skills-lock.json` | Tracked | Generated — plugin/skill version lock file | Whatever process maintains this lock file (not identified by name in this pass — requires locating the generating script before regenerating) | Unclear — generating script not identified in this pass | Unclear |
| 9 | `plugins/tradingview/assets/pinescript-indicators/registry.json` | Tracked | Unclear — registry of Pine Script indicators, could be hand-edited or generated by `pine_script_manager.py`/`tv_pine_manager.py` | Not identified in this pass | Unclear | Unclear |

## What Not to Overwrite

- Any file in the list above that has since been touched by other work in this session (e.g.
  `target-portfolio.json`, `thesis_breaker_state.json`, `ta-sweep-results.json` were all
  re-verified as clean/at-last-commit-state during this corrective pass — check `git status`
  again immediately before regenerating any of them, since state may have changed).
- Do not run any regeneration command without the user first confirming which files they want
  regenerated, since some (`predictions.jsonl`, `target-portfolio.json`) cannot be restored to
  their exact prior content — regenerating produces *new*, *current* data, not the lost data.

## Summary

Of the 9 affected files: 3 are safely, exactly regenerable with no data-quality caveat
(`ta-sweep-results.json`, and the `AUTO_UPDATE` blocks in the thesis/sub-strategy markdown
files — #3, #6, #7). The rest either cannot be restored to their exact prior content
(`predictions.jsonl`, `thesis_breaker_state.json`'s historical timing) or have an
unidentified-in-this-pass generating process (`skills-lock.json`, the pinescript registry,
`target-portfolio.json`'s specific lost edit). No action has been taken on any of these —
this table is for the user's review and explicit approval before anything is regenerated.
