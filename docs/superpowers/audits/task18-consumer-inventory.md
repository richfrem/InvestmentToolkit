# Task 18 Consumer Inventory — Legacy JSON Readers/Writers

Generated: 2026-07-18. Per corrective instructions §7: Task 18 (rewiring existing readers/
writers of legacy JSON to the shared ledger/repository layer) has **not started**. This table
inventories known consumers; it is a starting point, not a complete or executed migration.

Entries marked `UNKNOWN_REQUIRES_REVIEW` reflect consumers named in the corrective instructions
that were **not independently verified in this pass** — they are queued for audit, not guessed
at. Entries with real evidence (file:line, verified this session or the prior audit run) are
marked accordingly.

| Consumer | Current data source | Legacy reference? | Target source | Action | Test required | Status |
|---|---|---|---|---|---|---|
| `plugins/tradingview/scripts/ta_sweep_batch.py:40,279-298` | `ta-sweep-results.json` (writes via `save_sweep_results()`) | Yes — verified via audit reference-linking (this pass) | `intelligence_event` (`event_type=TECHNICAL_SWEEP`), once Task 15 (Phase 3) exists | `MIGRATION_REQUIRED` | Yes — migration + regression test needed | Not started |
| `plugins/portfolio-advisor/scripts/daily_brief.py:37,48,407` | `ta-sweep-results.json` (reads, via `TA_SWEEP_PATH`) | Yes — verified via audit reference-linking (this pass) | Same as above, once migrated | `MIGRATION_REQUIRED` | Yes | Not started |
| `investment_screener/backend/py_services/compute_conviction_scores.py:67,283` (`_load_ta()`) | `ta-sweep-results.json` (reads) | Yes — verified via audit reference-linking (this pass) | Same as above | `MIGRATION_REQUIRED` | Yes | Not started |
| `plugins/stock-valuation/skills/stock_valuation/SKILL.md` | `intelligence.event_store` CLI (research writes) | No — already rewired | `intelligence_event` via ledger | `USES_LEDGER_REPOSITORY` | Already covered (Task 10 tests) | **Done** (PR #77) |
| `plugins/stock-valuation/skills/stock-research/SKILL.md` | `intelligence.event_store` CLI (research writes) | No — already rewired | `intelligence_event` via ledger | `USES_LEDGER_REPOSITORY` | Already covered (Task 10 tests) | **Done** (PR #77) |
| `investment_screener/backend/src/routes/docs.ts` (`GET /research/:filename`, `GET /research`) | Generated `.summary.md`/`.timeline.md` files (once they exist) or dated `.md` files (fallback) | Partial — reads both shapes by design (Task 8) | Already reads generated views where they exist | `USES_GENERATED_VIEW` | Already covered (Task 8 tests) | **Done** (PR #77) |
| `plugins/portfolio-advisor/scripts/weekly_review.py` | Not independently verified this pass | `UNKNOWN_REQUIRES_REVIEW` | TBD | `UNKNOWN_REQUIRES_REVIEW` | TBD | Not audited |
| `plugins/portfolio-advisor/scripts/generate_reports.py` | Not independently verified this pass | `UNKNOWN_REQUIRES_REVIEW` | TBD | `UNKNOWN_REQUIRES_REVIEW` | TBD | Not audited |
| Other plugin scripts (portfolio-advisor, tradingview, stock-valuation, etc. beyond those named above) | Not independently verified this pass | `UNKNOWN_REQUIRES_REVIEW` | TBD | `UNKNOWN_REQUIRES_REVIEW` | TBD | Not audited — `docs/superpowers/audits/json-discovery-audit.md`'s per-file inventory has partial reference data for each JSON file, but has not been re-organized into a per-consumer-script view |
| Other `SKILL.md` files beyond the 2 already rewired | Not independently verified this pass | `UNKNOWN_REQUIRES_REVIEW` | TBD | `UNKNOWN_REQUIRES_REVIEW` | TBD | Not audited |
| `investment_screener/backend/src/routes/*.ts` beyond `docs.ts` (e.g. `portfolio.ts`, `theses.ts`) | Not independently verified this pass | `UNKNOWN_REQUIRES_REVIEW` | TBD | `REMAINS_JSON_BY_DESIGN` (likely — portfolio/execution domain is explicitly out of scope per ADR-028) but not confirmed | TBD | Not audited |
| `investment_screener/frontend/src/**` | Not independently verified this pass | `UNKNOWN_REQUIRES_REVIEW` | TBD | `UNKNOWN_REQUIRES_REVIEW` | TBD | Not audited |

## How to complete this inventory

`docs/superpowers/audits/json-discovery-audit.json`'s `files[].known_producers` /
`known_consumers` / `doc_references` arrays already contain `{referencing_file, line,
confidence}` entries for every JSON file the fixed audit script found references to. This table
should be regenerated by inverting that data (group by `referencing_file` instead of by JSON
file) rather than manually re-auditing — that inversion was not done in this pass due to time
constraints, and is the fastest path to closing the `UNKNOWN_REQUIRES_REVIEW` rows above.

## Status

Queued, per corrective instructions §7's "created or explicitly queued" allowance. Not
complete. No broad refactor should start until every row above has a confirmed classification.
